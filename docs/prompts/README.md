# Prompts

Canonical prompt files used by the final validation scripts live with the runnable code in `scripts/03_llm_classify/`:

- `prompt.md`
- `prompt_loader.py`
- `coding_prompt.md`
- `judge_prompt.md`

They are kept there so the scripts can run without duplicating prompt text.
