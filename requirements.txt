# Runtime dependencies for the retained thesis replication pipeline.
# PDF/OCR tooling and notebook/test/dev packages were removed because they are
# not used by the final dataset or retained scripts.

# Scraping and HTTP
requests>=2.31.0
urllib3>=2.0.0

# HTML/text extraction
beautifulsoup4>=4.12.0
trafilatura>=1.8.0
chardet>=5.2.0

# Configuration and tabular data
pyyaml>=6.0.1
pandas>=2.1.0

# Filtering/progress helpers
flashtext>=2.7
tqdm>=4.66.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0

# LLM validation scripts
openai>=1.0.0
